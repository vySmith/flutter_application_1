name: namer_app
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: '>=2.19.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.3.0  # 使用最新版本
  english_words: ^4.0.0
  provider: ^6.1.2
  file_picker: ^9.0.2
  path_provider: ^2.1.5
  shared_preferences: ^2.5.2 # 添加 shared_preferences 依赖
  image_picker: ^1.1.2 # 使用最新版本，请查看 pub.dev 获取最新版本
  intl: ^0.17.0 # 用于日期格式化
  sqflite: ^2.4.2
  photo_view: ^0.15.0
  video_player: ^2.9.3
  url_launcher: ^6.3.1
  uuid: ^4.5.1
  path: ^1.9.1
  flutter_pdfview: ^1.4.0
  just_audio: ^0.9.46
  chewie: ^1.11.1
  dio: ^5.8.0+1
  permission_handler: ^12.0.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/default_avatar.jpg
 